language: android
jdk:
  - oraclejdk8

android:
    components:
        - tools
        - build-tools-25.0.2
        - android-25
        - android-6
        # for submodule
        - build-tools-24.0.3
        - android-24
        #
        - extra-android-m2repository
    licences:
        - '.+'

script:
    - ./gradlew assembleDebug assembleAndroidTest lint
